syntax = "proto3";

package sensor;

message TemperatureSensorMeasurement {
    string zone_id = 1;
    string timestamp = 2;
    float temperature = 3;
    float pressure = 4;
    float humidity = 5;
}